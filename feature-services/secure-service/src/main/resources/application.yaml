server:
  port: 8035

spring:
  application:
    name: secure-service
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8080/realms/auth-realm #(Spring Boot will: Fetch public key automatically, Validate JWT signature)

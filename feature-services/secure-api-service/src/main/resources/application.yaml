server:
  port: 8035

spring:
  application:
    name: secure-api-service
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8080/realms/auth-realm   #(This connects your app to Keycloak)
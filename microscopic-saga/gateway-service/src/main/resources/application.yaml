server:
  port: 8070
  forward-headers-strategy: framework

#jwt:
#  secret: Zgc8AwPaabHnZft2VyqIBmNxNjCDZnNMNdT2NFQGYlk=

resilience4j:
  circuitbreaker:
    instances:
      orderServiceCB:
        registerHealthIndicator: true
        slidingWindowSize: 10
        #minimumNumberOfCalls: 5
        failureRateThreshold: 50
        waitDurationInOpenState: 10s
        permittedNumberOfCallsInHalfOpenState: 3

management:
  endpoint:
    gateway:
      enabled: true
    health:
      show-details: always

  endpoints:
    web:
      exposure:
        include: "*"

  tracing:
    sampling:
      probability: 1.0

  zipkin:
    tracing:
      endpoint: http://localhost:9411/api/v2/spans

logging:
  level:
    org.springframework.cloud.gateway: DEBUG
    org.springframework.data.redis: DEBUG
    org.springframework.cloud.vault: DEBUG
  #pattern:
  #  level: "%5p [${spring.application.name}, traceId=%X{traceId}, spanId=%X{spanId}]"

spring:
  application:
    name: gateway-service

  data:
    redis:
      host: localhost
      port: 6379

  security:
    user:
      name: admin
      password: admin9
      roles: ADMIN

  config:
    import: vault://

  cloud:
    gateway:
      routes:
        - id: order-service
          uri: http://localhost:8081
          predicates:
            - Path=/orders/**
          filters:
            - name: RequestRateLimiter
              args:
                redis-rate-limiter.replenishRate: 5
                redis-rate-limiter.burstCapacity: 10
                key-resolver: "#{@userKeyResolver}"
            - name: CircuitBreaker
              args:
                name: orderServiceCB
                fallbackUri: forward:/fallback/order
    #vault:
    #  uri: http://localhost:8200
    #  scheme: http
    #  token: hvs.UgCWHrhcXKDVF49812lfXcQp #when vault will be started, copy Root Token value

    #  kv:
    #    enabled: true
    #    backend: secret
    #    default-context: jwt-secret